apiVersion: v1
kind: Service
metadata:
  name: {{ include "bso-project.fullname" . }}
  labels:
    {{- include "bso-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bso-project.selectorLabels" . | nindent 4 }}
