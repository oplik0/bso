apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bso-project.fullname" . }}-test-connection"
  labels:
    {{- include "bso-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bso-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
